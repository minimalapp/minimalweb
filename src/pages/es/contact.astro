---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
import PageHero from '@/components/PageHero.astro';
import ContentSection from '@/components/ContentSection.astro';
import SectionTitle from '@/components/SectionTitle.astro';
import FAQ from '@/components/FAQ.astro';
import Button from '@/components/Button.astro';
import InfoCard from '@/components/InfoCard.astro';

const locale = 'es';

const faqItems = [
  {
    question: "¿Qué tan rápido puedo esperar una respuesta?",
    answer: "<p>Típicamente respondemos a todas las consultas dentro de 24 horas durante días hábiles. Para solicitudes de soporte técnico, nuestro tiempo promedio de respuesta es de menos de 2 horas. Los problemas urgentes se priorizan y se manejan de inmediato.</p>"
  },
  {
    question: "¿Puedo programar una demostración de Minimal?",
    answer: "<p>¡Por supuesto! Selecciona \"Solicitar Demo\" del menú desplegable de asunto al completar el formulario de contacto. Nuestro equipo se pondrá en contacto para programar una demostración personalizada a tu conveniencia. Te guiaremos por todas las funciones y responderemos cualquier pregunta que tengas.</p>"
  },
  {
    question: "¿Ofrecen soluciones empresariales personalizadas?",
    answer: "<p>¡Sí! Proporcionamos soluciones empresariales personalizadas con soporte dedicado, integraciones personalizadas y garantías de SLA. Selecciona \"Oportunidad de Asociación\" en el formulario de contacto, y nuestro equipo empresarial discutirá tus requisitos específicos y creará una solución a medida.</p>"
  },
  {
    question: "¿Qué información debo incluir en mi solicitud de soporte?",
    answer: `<p>Para ayudarnos a asistirte rápidamente, por favor incluye:</p>
      <ul>
        <li>La URL de tu sitio web y el ID del sitio</li>
        <li>Información del navegador y dispositivo</li>
        <li>Descripción detallada del problema</li>
        <li>Capturas de pantalla o grabaciones si es aplicable</li>
        <li>Cualquier mensaje de error que estés viendo</li>
      </ul>`
  },
  {
    question: "¿Hay un número de teléfono al que pueda llamar?",
    answer: "<p>Apoyamos principalmente a los clientes a través de correo electrónico para asegurar respuestas de calidad. Para clientes empresariales, proporcionamos soporte telefónico dedicado como parte de tu plan. Contáctanos en hello@minimal.lat para discutir tus necesidades.</p>"
  }
];
---

<BaseLayout title="Contacto - Minimal" description="Ponte en contacto con el equipo de Minimal." locale={locale}>
  <Header locale={locale} />

  <main class="pt-header">
    <PageHero
      title="Contáctanos"
      subtitle="¿Tienes preguntas? Nos encantaría escuchar de ti. Envíanos un mensaje y responderemos lo antes posible."
    />

    <ContentSection>
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-start">
        <!-- Form -->
        <div>
          <SectionTitle title="Envíanos un Mensaje" size="md" spacing="normal" />
          <form class="flex flex-col gap-6" id="contact-form-es">
            <div class="flex flex-col gap-2">
              <label for="full_name" class="text-[var(--text-primary)] text-base font-semibold">Nombre Completo *</label>
              <input type="text" id="full_name" name="full_name" placeholder="Juan Pérez" required
                class="px-6 py-4 min-h-[52px] border-2 border-[var(--border-primary)] rounded-lg text-base bg-[var(--bg-primary)] text-[var(--text-primary)] transition-all duration-200 shadow-sm focus:outline-none focus:border-[var(--brand-purple-primary)] focus:shadow-[0_0_0_3px_rgba(99,102,241,0.1)]" />
            </div>

            <div class="flex flex-col gap-2">
              <label for="email" class="text-[var(--text-primary)] text-base font-semibold">Correo Electrónico *</label>
              <input type="email" id="email" name="email" placeholder="juan@ejemplo.com" required
                class="px-6 py-4 min-h-[52px] border-2 border-[var(--border-primary)] rounded-lg text-base bg-[var(--bg-primary)] text-[var(--text-primary)] transition-all duration-200 shadow-sm focus:outline-none focus:border-[var(--brand-purple-primary)] focus:shadow-[0_0_0_3px_rgba(99,102,241,0.1)]" />
            </div>

            <div class="flex flex-col gap-2">
              <label for="company" class="text-[var(--text-primary)] text-base font-semibold">Empresa</label>
              <input type="text" id="company" name="company" placeholder="Nombre de tu Empresa"
                class="px-6 py-4 min-h-[52px] border-2 border-[var(--border-primary)] rounded-lg text-base bg-[var(--bg-primary)] text-[var(--text-primary)] transition-all duration-200 shadow-sm focus:outline-none focus:border-[var(--brand-purple-primary)] focus:shadow-[0_0_0_3px_rgba(99,102,241,0.1)]" />
            </div>

            <div class="flex flex-col gap-2">
              <label for="website" class="text-[var(--text-primary)] text-base font-semibold">Sitio Web</label>
              <input type="url" id="website" name="website" placeholder="https://ejemplo.com"
                class="px-6 py-4 min-h-[52px] border-2 border-[var(--border-primary)] rounded-lg text-base bg-[var(--bg-primary)] text-[var(--text-primary)] transition-all duration-200 shadow-sm focus:outline-none focus:border-[var(--brand-purple-primary)] focus:shadow-[0_0_0_3px_rgba(99,102,241,0.1)]" />
            </div>

            <div class="flex flex-col gap-2">
              <label for="subject" class="text-[var(--text-primary)] text-base font-semibold">Asunto *</label>
              <select id="subject" name="subject" required
                class="px-6 py-4 min-h-[52px] border-2 border-[var(--border-primary)] rounded-lg text-base bg-[var(--bg-primary)] text-[var(--text-primary)] transition-all duration-200 shadow-sm cursor-pointer appearance-none bg-[url('data:image/svg+xml,%3Csvg%20xmlns=%22http://www.w3.org/2000/svg%22%20width=%2224%22%20height=%2224%22%20viewBox=%220%200%2024%2024%22%20fill=%22none%22%20stroke=%22%236b7280%22%20stroke-width=%222%22%3E%3Cpolyline%20points=%226%209%2012%2015%2018%209%22%3E%3C/polyline%3E%3C/svg%3E')] bg-no-repeat bg-[right_12px_center] bg-[length:20px] pr-12 focus:outline-none focus:border-[var(--brand-purple-primary)] focus:shadow-[0_0_0_3px_rgba(99,102,241,0.1)]">
                <option value="">Selecciona una opción</option>
                <option value="demo">Solicitar Demo</option>
                <option value="pricing">Información de Precios</option>
                <option value="integration">Pregunta de Integración</option>
                <option value="support">Soporte Técnico</option>
                <option value="partnership">Oportunidad de Asociación</option>
                <option value="other">Otro</option>
              </select>
            </div>

            <div class="flex flex-col gap-2">
              <label for="message" class="text-[var(--text-primary)] text-base font-semibold">Mensaje *</label>
              <textarea id="message" name="message" placeholder="Cuéntanos cómo podemos ayudarte..." rows="6" required minlength="10"
                class="px-6 py-4 min-h-[150px] border-2 border-[var(--border-primary)] rounded-lg text-base bg-[var(--bg-primary)] text-[var(--text-primary)] transition-all duration-200 shadow-sm resize-y focus:outline-none focus:border-[var(--brand-purple-primary)] focus:shadow-[0_0_0_3px_rgba(99,102,241,0.1)]"></textarea>
            </div>

            <!-- Form message container -->
            <div id="form-message-es" class="hidden rounded-lg p-4 text-sm"></div>

            <Button type="submit" variant="primary" size="lg" className="w-full">
              Enviar Mensaje
            </Button>
          </form>
        </div>

        <!-- Contact Info Cards -->
        <div>
          <SectionTitle title="Ponte en Contacto" size="md" spacing="normal" />

          <div class="mb-6 text-center">
            <InfoCard
              icon="envelope"
              title="Envíanos un Correo"
              description="Para consultas generales: hello@minimal.lat"
              interactive={true}
            />
          </div>

          <div class="mb-6 text-center">
            <InfoCard
              icon="wrench"
              title="Soporte Técnico"
              description="¿Necesitas ayuda con la integración? support@minimal.lat"
              interactive={true}
            />
          </div>
        </div>
      </div>
    </ContentSection>

    <ContentSection background="secondary">
      <SectionTitle title="Preguntas Frecuentes" centered={true} />
      <FAQ items={faqItems} />
    </ContentSection>
  </main>

  <Footer locale={locale} />
</BaseLayout>

<script>
  import { trackContactFormSubmit } from '@/scripts/analytics';

  const API_URL = import.meta.env.PUBLIC_BACKEND_URL || 'http://localhost:8000';
  const form = document.getElementById('contact-form-es') as HTMLFormElement;
  const submitBtn = form?.querySelector('button[type="submit"]') as HTMLButtonElement;
  const messageContainer = document.getElementById('form-message-es') as HTMLDivElement;
  const originalBtnText = submitBtn?.textContent || 'Enviar Mensaje';

  function showMessage(type: 'success' | 'error', message: string) {
    if (!messageContainer) return;

    messageContainer.className = type === 'success'
      ? 'rounded-lg p-4 text-sm bg-green-50 border border-green-200 text-green-800'
      : 'rounded-lg p-4 text-sm bg-red-50 border border-red-200 text-red-800';

    messageContainer.innerHTML = message;
    messageContainer.classList.remove('hidden');
    messageContainer.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }

  function hideMessage() {
    if (messageContainer) {
      messageContainer.classList.add('hidden');
    }
  }

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    hideMessage();

    const formData = new FormData(form);
    const subject = formData.get('subject') as string;

    const payload = {
      full_name: formData.get('full_name'),
      email: formData.get('email'),
      company: formData.get('company') || '',
      website: formData.get('website') || '',
      subject: subject || 'other',
      message: formData.get('message'),
    };

    // Disable button and show loading state
    if (submitBtn) {
      submitBtn.disabled = true;
      submitBtn.textContent = 'Enviando...';
    }

    try {
      const response = await fetch(`${API_URL}/api/contact/`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(payload),
      });

      const data = await response.json();

      if (response.ok) {
        // Track successful submission
        trackContactFormSubmit(subject || 'not_specified', '/es/contact');

        // Show success message
        showMessage('success', `
          <div class="flex items-start gap-3">
            <svg class="w-5 h-5 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <div>
              <p class="font-medium">¡Mensaje enviado con éxito!</p>
              <p class="mt-1 opacity-90">${data.message || "Gracias por contactarnos. Te responderemos pronto."}</p>
            </div>
          </div>
        `);
        form.reset();
      } else {
        // Handle validation errors
        const fieldLabels: Record<string, string> = {
          full_name: 'Nombre completo',
          email: 'Correo electrónico',
          company: 'Empresa',
          website: 'Sitio web',
          subject: 'Asunto',
          message: 'Mensaje'
        };

        if (data.details) {
          const errorList = Object.entries(data.details)
            .map(([field, msgs]) => `<li><strong>${fieldLabels[field] || field}:</strong> ${(msgs as string[]).join(', ')}</li>`)
            .join('');
          showMessage('error', `
            <div class="flex items-start gap-3">
              <svg class="w-5 h-5 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
              </svg>
              <div>
                <p class="font-medium">Por favor corrige los siguientes errores:</p>
                <ul class="mt-1 list-disc list-inside opacity-90">${errorList}</ul>
              </div>
            </div>
          `);
        } else {
          showMessage('error', `
            <div class="flex items-start gap-3">
              <svg class="w-5 h-5 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
              </svg>
              <div>
                <p class="font-medium">Algo salió mal</p>
                <p class="mt-1 opacity-90">${data.error || 'Por favor intenta de nuevo más tarde.'}</p>
              </div>
            </div>
          `);
        }
      }
    } catch (error) {
      console.error('Contact form error:', error);
      showMessage('error', `
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
          </svg>
          <div>
            <p class="font-medium">Error de conexión</p>
            <p class="mt-1 opacity-90">Por favor verifica tu conexión a internet e intenta de nuevo.</p>
          </div>
        </div>
      `);
    } finally {
      // Re-enable button
      if (submitBtn) {
        submitBtn.disabled = false;
        submitBtn.textContent = originalBtnText;
      }
    }
  });
</script>
